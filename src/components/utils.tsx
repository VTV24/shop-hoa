import styled from '@emotion/styled';

export const FlowerCardContainer = styled.div`
    width: 100%;
    display: grid;
    grid-template-columns: repeat(auto-fit, min(300px, 100%));
    grid-gap: 1rem;
`;
